import { Button, VStack } from '@lidofinance/lido-ui';
import React, { FC } from 'react';

const DocumentationLinkComponent: FC = () => (
  <VStack align="center" justify="center">
  </VStack>
);

export const DocumentationLink = React.memo(DocumentationLinkComponent);
